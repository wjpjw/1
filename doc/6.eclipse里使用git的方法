一.前中期团队使用eclipse的内置git的方法可以较为简单地总结如下：
1. 我们的系统分为3个依赖较小的子系统，所以可以采用3个分支的并行开发。
2. 我已经为每个人创建了3个分支，master是主线，只存放稳定版本，我定期将其他私人分支的代码merge到master。
3. 这种使用方法的优势是：每个人都不需要让自己的分支和master保持一致，不必pull，因为对其他人的代码暂时无依赖。

二.在开发进入后期集成优化环节，我们就得每次开发都把自己分支从master那里pull，保持一致。
